[project]
name = "aigc-core"
version = "0.0.1"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = ["python-dotenv",
                "datasets>=4.5.0",
                "peft>=0.18.1",
                "pip>=25.3",
                "swanlab>=0.7.6",
                "torch>=2.10.0"]

[tool.setuptools.packages.find]
include = ["aigcore*"]  # 只包含 aigcore 开头的包
exclude = ["examples*", "runs*"] # 排除干扰项